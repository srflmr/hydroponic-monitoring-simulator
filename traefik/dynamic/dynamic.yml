http:
  routers:
    authelia:
      rule: "Host(`auth.hydroponic.localhost`)"
      entryPoints:
        - websecure
      service: authelia
      tls: {}

    dashboard-ws:
      rule: "Host(`hydroponic.localhost`) && PathPrefix(`/socket.io`)"
      entryPoints:
        - websecure
      service: dashboard-ws
      middlewares:
        - authelia
      tls: {}

  services:
    authelia:
      loadBalancer:
        servers:
          - url: "http://authelia:9091"

    dashboard-ws:
      loadBalancer:
        servers:
          - url: "http://dashboard-api:3010"

  middlewares:
    authelia:
      forwardAuth:
        address: "http://authelia:9091/api/authz/forward-auth"
        trustForwardHeader: true
        authResponseHeaders:
          - Remote-User
          - Remote-Groups
          - Remote-Name
          - Remote-Email
